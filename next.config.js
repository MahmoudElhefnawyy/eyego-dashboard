/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here if you have any.
  // For this project, an empty object is sufficient.
};

module.exports = nextConfig;
